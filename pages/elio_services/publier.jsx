import LayoutDashboard from "@/components/admin/LayoutDashboard";

const PublierService = () => {
  return (
    <LayoutDashboard>
      <h1 className="text-2xl font-bold mb-4">publier service</h1>
      {/* <p>Interface pour suivre les paiements, statuts, relances, etc.</p> */}
    </LayoutDashboard>
  );
};

export default PublierService;
